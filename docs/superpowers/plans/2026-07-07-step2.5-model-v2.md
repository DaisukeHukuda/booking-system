# 予約管理システム ステップ2.5（モデル拡張 v2）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** スペックv2の4要件 — ①時間の重なり連動 ②日別定員上書き ③リクエスト予約（承認/否認） ④大人/小人の料金内訳 — をスキーマ・コアロジック・管理画面に実装する。

**Architecture:** 導出型空き状況＋アトミック1文SQL の設計は不変。変更は (a) スキーマv2（0001_init.sql を書き直し — 本番未デプロイのため。スペック§7参照）、(b) コアの空き条件の強化、(c) 管理画面の追随。

**参照:** スペック `docs/superpowers/specs/2026-07-07-booking-system-design.md` §3〜§5（v2）
**前提:** ステップ2完了（68テストgreen）。ブランチ `step2.5-model-v2` を main から切る。

**タスク構成:** T1=挙動を変えないモデル移行（列追加・改名・型変更） → T2=コアv2ロジック → T3=予約フォームの内訳対応 → T4=承認フローと日別定員のUI → T5=仕上げ。**T1とT2の間以外は各タスク末で全テストgreenを維持する。**

---

### Task 1: モデル移行（挙動不変）

スキーマ・型・フィクスチャ・コア関数のシグネチャを v2 に移行する。**このタスクでは判定ロジックは変えない**（重なり連動・定員上書き・requested の扱いは Task 2）。

**Files:**
- Rewrite: `migrations/0001_init.sql`
- Modify: `src/types.ts`, `src/core/booking.ts`, `src/routes/admin/calendar.tsx`（最小shim）, `src/routes/admin/plans.tsx`（最小shim）, `test/fixtures.ts`, `test/schema.test.ts`, `test/availability.test.ts`, `test/booking.test.ts`, `test/admin-edit.test.ts`

- [ ] **Step 1: スキーマv2**

`migrations/0001_init.sql` の該当部分を変更（他のテーブルはそのまま）:

`plans` を置き換え:
```sql
CREATE TABLE plans (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  description TEXT NOT NULL DEFAULT '',
  price_adult INTEGER NOT NULL DEFAULT 0,
  price_child INTEGER NOT NULL DEFAULT 0,
  duration_min INTEGER NOT NULL DEFAULT 120 CHECK (duration_min > 0),
  active INTEGER NOT NULL DEFAULT 1,
  sort_order INTEGER NOT NULL DEFAULT 0
);
```

`slot_closures` の後に追加:
```sql
CREATE TABLE capacity_overrides (
  date TEXT NOT NULL,
  plan_id INTEGER NOT NULL REFERENCES plans(id),
  slot_type_id INTEGER NOT NULL REFERENCES slot_types(id),
  capacity INTEGER NOT NULL CHECK (capacity >= 0),
  PRIMARY KEY (date, plan_id, slot_type_id)
);
```

`agencies` に列追加（token の後）:
```sql
  booking_mode TEXT NOT NULL DEFAULT 'request' CHECK (booking_mode IN ('realtime', 'request')),
```

`bookings` の status 行と party_size 周辺を変更:
```sql
  status TEXT NOT NULL DEFAULT 'confirmed' CHECK (status IN ('requested', 'confirmed', 'cancelled', 'denied')),
  customer_name TEXT NOT NULL,
  customer_phone TEXT NOT NULL DEFAULT '',
  num_adults INTEGER NOT NULL DEFAULT 0 CHECK (num_adults >= 0),
  num_children INTEGER NOT NULL DEFAULT 0 CHECK (num_children >= 0),
  price_adult INTEGER NOT NULL DEFAULT 0,
  price_child INTEGER NOT NULL DEFAULT 0,
  party_size INTEGER NOT NULL CHECK (party_size > 0),
```

- [ ] **Step 2: 型の更新**

`src/types.ts`:
```ts
export type BookingStatus = 'requested' | 'confirmed' | 'cancelled' | 'denied';

export interface NewBooking {
  planId: number;
  date: string;
  slotTypeId: number;
  agencyId?: number | null;
  customerName: string;
  customerPhone?: string;
  numAdults: number;
  numChildren: number;
  priceAdult: number;   // 予約時点の単価スナップショット
  priceChild: number;
  totalAmount: number;
  paymentMethod: PaymentMethod;
  notes?: string;
  createdBy: 'admin' | 'agency';
  status?: 'confirmed' | 'requested'; // 省略時 confirmed
}
```
（`partySize` を削除。`Bindings`/`BookingResult`/`SlotStatus`/`SlotAvailability`/`PaymentMethod` は不変）

- [ ] **Step 3: フィクスチャ更新**

`test/fixtures.ts` を書き換え:
```ts
import type { NewBooking } from '../src/types';

// シナリオ: プランA・B・Cはインストラクター1を共有（コース連動）。プランDは独立リソース（ボート1）。
// 時刻: 1=午前便09:00 / 2=午後便13:00 / 3=10時半便10:30。所要時間は全プラン120分。
// → A@09:00(〜11:00) と B@10:30(〜12:30) は重なる。A@13:00 と B@10:30 は重ならない。
export const PLAN_A = 1;
export const PLAN_B = 2;
export const PLAN_C = 3;
export const PLAN_D = 4;
export const SLOT_AM = 1;
export const SLOT_PM = 2;
export const SLOT_1030 = 3;
export const AGENCY_1 = 1;
export const CAP_A = 6;
export const CAP_B = 4;

export async function seedBasic(db: D1Database): Promise<void> {
  await db.batch([
    db.prepare(`INSERT INTO resources (id, name) VALUES (1, 'インストラクター1'), (2, 'ボート1')`),
    db.prepare(`INSERT INTO plans (id, name, price_adult, price_child, duration_min) VALUES
      (1, 'プランA', 8000, 4000, 120), (2, 'プランB', 10000, 5000, 120),
      (3, 'プランC', 12000, 6000, 120), (4, 'プランD', 6000, 3000, 120)`),
    db.prepare(`INSERT INTO plan_resources (plan_id, resource_id) VALUES (1, 1), (2, 1), (3, 1), (4, 2)`),
    db.prepare(`INSERT INTO slot_types (id, name, start_time, sort_order) VALUES
      (1, '午前便', '09:00', 1), (2, '午後便', '13:00', 3), (3, '10時半便', '10:30', 2)`),
    db.prepare(`INSERT INTO plan_slots (plan_id, slot_type_id, capacity) VALUES
      (1, 1, 6), (1, 2, 6), (2, 1, 4), (2, 2, 4), (2, 3, 4), (3, 1, 6), (3, 2, 6), (4, 1, 8), (4, 2, 8)`),
    db.prepare(`INSERT INTO agencies (id, name, token, email, booking_mode, created_at) VALUES
      (1, 'テスト代理店', 'test-agency-token-0123456789abcdef', 'agency@example.com', 'request', '2026-07-07T00:00:00.000Z')`)
  ]);
}

export function makeBooking(overrides: Partial<NewBooking> = {}): NewBooking {
  return {
    planId: PLAN_A,
    date: '2026-08-01',
    slotTypeId: SLOT_AM,
    customerName: 'テスト太郎',
    customerPhone: '090-0000-0000',
    numAdults: 2,
    numChildren: 0,
    priceAdult: 8000,
    priceChild: 4000,
    totalAmount: 16000,
    paymentMethod: 'onsite_cash',
    createdBy: 'admin',
    ...overrides
  };
}
```

- [ ] **Step 4: コアのシグネチャ移行（判定ロジック不変）**

`src/core/booking.ts`:
- `slotOpenCond` の引数を `{ planId, date, slotTypeId, partySize }` のまま維持（呼び出し側で合計を渡す）。**SQL条件は現状のまま**
- `createBooking`: `party_size` へ `nb.numAdults + nb.numChildren` を、`num_adults`/`num_children`/`price_adult`/`price_child` を各列へ、`status` へ `nb.status ?? 'confirmed'` を INSERT する（SELECT句の 'confirmed' リテラルをパラメータに変更）。`numAdults + numChildren < 1` なら DB に触らず `{ ok: false, reason: 'slot_unavailable' }` を返す
- `BookingChange` を `{ planId, date, slotTypeId, numAdults, numChildren, totalAmount?, notes? }` に変更。`changeBooking` は `party_size = numAdults + numChildren`、`num_adults`/`num_children` も UPDATE する

`src/core/availability.ts`: 変更なし（`booked` は party_size 合計のまま）。

- [ ] **Step 5: UIの最小shim（フォームは既存のまま動かす）**

- `src/routes/admin/calendar.tsx`: `createBooking`/`changeBooking` 呼び出しを新型に合わせる。フォームの `party_size` 値を `numAdults` に入れ、`numChildren: 0`、`priceAdult`/`priceChild` はプランから引く（`SELECT price_adult, price_child FROM plans WHERE id = ?`。プランが無ければ invalid 扱い）。編集フォームの表示値は `num_adults` 列ではなく従来どおり `party_size` を表示してよい（Task 3 で正式対応）
- `src/routes/admin/plans.tsx`: `price` 列参照をすべて `price_adult` に変更（フォームのフィールド名は `price` のまま、一覧表示も価格数値のみでテスト互換維持）。INSERT/UPDATE は `price_adult` に書く

- [ ] **Step 6: 既存テストの機械的更新**

- `test/schema.test.ts`: 期待テーブル一覧に `capacity_overrides` を追加（アルファベット順で `bookings` の後）
- `test/availability.test.ts`: `toHaveLength(8)` → `(9)`、`toHaveLength(8 * 3)` → `(9 * 3)`（plan_slots が9行になったため）
- `test/booking.test.ts`: `makeBooking({ partySize: N })` 形式をすべて `makeBooking({ numAdults: N })` に置換。`changeBooking` の引数 `partySize: N` を `numAdults: N, numChildren: 0` に置換。それ以外の期待値は不変
- `test/admin-edit.test.ts`: `changeBooking` は使っていないので不変のはず。壊れたら最小修正して報告

- [ ] **Step 7: 全テスト green 確認** — Run: `npm test && npm run typecheck`（68件前後・全green）
- [ ] **Step 8: Commit** — `refactor: モデルv2移行(料金内訳・所要時間・定員上書き表・status4値) 挙動不変`

---

### Task 2: コアv2ロジック（重なり連動・日別定員・リクエスト予約）

**このタスクのコードは設計済み。一字一句そのまま使うこと。**

**Files:**
- Modify: `src/core/booking.ts`, `src/core/availability.ts`
- Create: `test/core-v2.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

`test/core-v2.test.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { env } from 'cloudflare:test';
import { createBooking, cancelBooking, approveBooking, denyBooking } from '../src/core/booking';
import { getAvailability } from '../src/core/availability';
import { seedBasic, makeBooking, PLAN_A, PLAN_B, PLAN_D, SLOT_AM, SLOT_PM, SLOT_1030 } from './fixtures';

const D = '2026-08-01';

function slot(avail: Awaited<ReturnType<typeof getAvailability>>, planId: number, slotTypeId: number) {
  const found = avail.find((a) => a.planId === planId && a.date === D && a.slotTypeId === slotTypeId);
  if (!found) throw new Error(`slot not found: plan=${planId} slot=${slotTypeId}`);
  return found;
}

describe('時間の重なり連動', () => {
  beforeEach(async () => {
    await seedBasic(env.DB);
  });

  it('A@09:00(120分)の予約で、重なるB@10:30がクローズし、重ならないB@13:00は空きのまま', async () => {
    expect((await createBooking(env.DB, makeBooking({ planId: PLAN_A, slotTypeId: SLOT_AM }))).ok).toBe(true);
    expect((await createBooking(env.DB, makeBooking({ planId: PLAN_B, slotTypeId: SLOT_1030, customerName: '重なり客' }))).ok).toBe(false);
    expect((await createBooking(env.DB, makeBooking({ planId: PLAN_B, slotTypeId: SLOT_PM, customerName: '午後の客' }))).ok).toBe(true);
  });

  it('A@13:00の予約は、B@10:30(〜12:30)と重ならないので両立できる', async () => {
    expect((await createBooking(env.DB, makeBooking({ planId: PLAN_A, slotTypeId: SLOT_PM }))).ok).toBe(true);
    expect((await createBooking(env.DB, makeBooking({ planId: PLAN_B, slotTypeId: SLOT_1030, customerName: '別件' }))).ok).toBe(true);
  });

  it('空き状況にも重なり連動が反映され、原因プランが分かる', async () => {
    await createBooking(env.DB, makeBooking({ planId: PLAN_A, slotTypeId: SLOT_AM }));
    const avail = await getAvailability(env.DB, D, D);
    expect(slot(avail, PLAN_B, SLOT_1030).status).toBe('linked_closed');
    expect(slot(avail, PLAN_B, SLOT_1030).blockingPlanIds).toEqual([PLAN_A]);
    expect(slot(avail, PLAN_B, SLOT_PM).status).toBe('open');
    expect(slot(avail, PLAN_D, SLOT_AM).status).toBe('open'); // リソース非共有
  });

  it('リソースを共有しないプランは時間が重なっても影響しない', async () => {
    await createBooking(env.DB, makeBooking({ planId: PLAN_D, slotTypeId: SLOT_AM }));
    expect((await createBooking(env.DB, makeBooking({ planId: PLAN_B, slotTypeId: SLOT_1030, customerName: '別件' }))).ok).toBe(true);
  });
});

describe('日別定員上書き', () => {
  beforeEach(async () => {
    await seedBasic(env.DB);
  });

  async function setOverride(planId: number, slotTypeId: number, capacity: number) {
    await env.DB.prepare(
      `INSERT INTO capacity_overrides (date, plan_id, slot_type_id, capacity) VALUES (?, ?, ?, ?)`
    ).bind(D, planId, slotTypeId, capacity).run();
  }

  it('上書き定員が基本定員より小さい場合、そちらが効く', async () => {
    await setOverride(PLAN_A, SLOT_AM, 2); // 基本6 → 2
    expect((await createBooking(env.DB, makeBooking({ numAdults: 2 }))).ok).toBe(true);
    expect((await createBooking(env.DB, makeBooking({ numAdults: 1, customerName: '超過客' }))).ok).toBe(false);
  });

  it('上書き定員が基本定員より大きい場合も、そちらが効く', async () => {
    await setOverride(PLAN_B, SLOT_AM, 10); // 基本4 → 10
    expect((await createBooking(env.DB, makeBooking({ planId: PLAN_B, numAdults: 5 }))).ok).toBe(true);
  });

  it('空き状況の capacity/remaining に上書きが反映される', async () => {
    await setOverride(PLAN_A, SLOT_AM, 2);
    const avail = await getAvailability(env.DB, D, D);
    const a = avail.find((x) => x.planId === PLAN_A && x.slotTypeId === SLOT_AM)!;
    expect(a.capacity).toBe(2);
    expect(a.remaining).toBe(2);
  });

  it('別の日には影響しない', async () => {
    await setOverride(PLAN_A, SLOT_AM, 2);
    expect((await createBooking(env.DB, makeBooking({ date: '2026-08-02', numAdults: 5 }))).ok).toBe(true);
  });
});

describe('リクエスト予約', () => {
  beforeEach(async () => {
    await seedBasic(env.DB);
  });

  it('requestedで登録でき、在庫を押さえる（連動・定員とも）', async () => {
    const req = await createBooking(env.DB, makeBooking({ status: 'requested' }));
    expect(req.ok).toBe(true);
    // 連動: 同時間帯のBは不可
    expect((await createBooking(env.DB, makeBooking({ planId: PLAN_B, customerName: '別件' }))).ok).toBe(false);
    // 定員: A残り4に5名は不可
    expect((await createBooking(env.DB, makeBooking({ numAdults: 5, customerName: '定員客' }))).ok).toBe(false);
    // 空き状況のbookedにも含まれる
    const avail = await getAvailability(env.DB, D, D);
    expect(avail.find((a) => a.planId === PLAN_A && a.slotTypeId === SLOT_AM)?.booked).toBe(2);
  });

  it('承認すると confirmed になる。confirmed の再承認は false', async () => {
    const req = await createBooking(env.DB, makeBooking({ status: 'requested' }));
    if (!req.ok) throw new Error('setup failed');
    expect(await approveBooking(env.DB, req.bookingId)).toBe(true);
    const row = await env.DB.prepare(`SELECT status FROM bookings WHERE id = ?`).bind(req.bookingId).first<{ status: string }>();
    expect(row?.status).toBe('confirmed');
    expect(await approveBooking(env.DB, req.bookingId)).toBe(false);
  });

  it('否認すると denied になり、枠が即座に戻る', async () => {
    const req = await createBooking(env.DB, makeBooking({ status: 'requested' }));
    if (!req.ok) throw new Error('setup failed');
    expect(await denyBooking(env.DB, req.bookingId)).toBe(true);
    const row = await env.DB.prepare(`SELECT status, cancelled_at FROM bookings WHERE id = ?`).bind(req.bookingId).first<{ status: string; cancelled_at: string | null }>();
    expect(row?.status).toBe('denied');
    expect(row?.cancelled_at).not.toBeNull();
    expect((await createBooking(env.DB, makeBooking({ planId: PLAN_B, customerName: '別件' }))).ok).toBe(true);
  });

  it('requested もキャンセルできる', async () => {
    const req = await createBooking(env.DB, makeBooking({ status: 'requested' }));
    if (!req.ok) throw new Error('setup failed');
    expect(await cancelBooking(env.DB, req.bookingId)).toBe(true);
  });

  it('confirmed の否認は false', async () => {
    const c = await createBooking(env.DB, makeBooking());
    if (!c.ok) throw new Error('setup failed');
    expect(await denyBooking(env.DB, c.bookingId)).toBe(false);
  });
});
```

- [ ] **Step 2: 失敗確認** — Run: `npm test -- core-v2` → FAIL

- [ ] **Step 3: コア実装（以下のコードをそのまま使う）**

`src/core/booking.ts` の `slotOpenCond` を以下で**置き換える**:
```ts
// SQL内で 'HH:MM' を分に変換する式
const MIN = (col: string) =>
  `(CAST(substr(${col}, 1, 2) AS INTEGER) * 60 + CAST(substr(${col}, 4, 2) AS INTEGER))`;

// 予約可能条件（WHERE句の断片）。スペック§4 v2 の①〜④に対応。
// excludeBookingId は予約変更時に「自分自身の予約」を人数・リソース判定から除外するために使う。
function slotOpenCond(
  v: { planId: number; date: string; slotTypeId: number; partySize: number },
  excludeBookingId: number | null
): { sql: string; params: (string | number | null)[] } {
  const ex = excludeBookingId;
  return {
    sql: `
      EXISTS (SELECT 1 FROM plan_slots ps JOIN plans p ON p.id = ps.plan_id
              WHERE ps.plan_id = ? AND ps.slot_type_id = ? AND ps.active = 1 AND p.active = 1)
      AND NOT EXISTS (SELECT 1 FROM slot_closures sc
              WHERE sc.date = ? AND sc.slot_type_id = ? AND (sc.plan_id IS NULL OR sc.plan_id = ?))
      AND (SELECT COALESCE(SUM(b.party_size), 0) FROM bookings b
              WHERE b.plan_id = ? AND b.date = ? AND b.slot_type_id = ?
                AND b.status IN ('requested', 'confirmed')
                AND (? IS NULL OR b.id != ?)) + ?
          <= COALESCE(
               (SELECT co.capacity FROM capacity_overrides co
                 WHERE co.date = ? AND co.plan_id = ? AND co.slot_type_id = ?),
               (SELECT ps.capacity FROM plan_slots ps WHERE ps.plan_id = ? AND ps.slot_type_id = ?))
      AND NOT EXISTS (SELECT 1 FROM bookings b
              JOIN slot_types st_b ON st_b.id = b.slot_type_id
              JOIN plans p_b ON p_b.id = b.plan_id
              JOIN plan_resources pr_o ON pr_o.plan_id = b.plan_id
              JOIN plan_resources pr_m ON pr_m.resource_id = pr_o.resource_id
              WHERE pr_m.plan_id = ? AND b.date = ?
                AND b.status IN ('requested', 'confirmed')
                AND NOT (b.plan_id = ? AND b.slot_type_id = ?)
                AND (? IS NULL OR b.id != ?)
                AND ${MIN('st_b.start_time')}
                    < (SELECT ${MIN('st.start_time')} FROM slot_types st WHERE st.id = ?)
                      + (SELECT p.duration_min FROM plans p WHERE p.id = ?)
                AND (SELECT ${MIN('st.start_time')} FROM slot_types st WHERE st.id = ?)
                    < ${MIN('st_b.start_time')} + p_b.duration_min)`,
    params: [
      v.planId, v.slotTypeId,
      v.date, v.slotTypeId, v.planId,
      v.planId, v.date, v.slotTypeId, ex, ex, v.partySize,
      v.date, v.planId, v.slotTypeId, v.planId, v.slotTypeId,
      v.planId, v.date, v.planId, v.slotTypeId, ex, ex, v.slotTypeId, v.planId, v.slotTypeId
    ]
  };
}
```

`cancelBooking` の WHERE を `status IN ('confirmed', 'requested')` に変更し、以下を追加:
```ts
export async function approveBooking(db: D1Database, bookingId: number): Promise<boolean> {
  const res = await db.prepare(
    `UPDATE bookings SET status = 'confirmed' WHERE id = ? AND status = 'requested'`
  ).bind(bookingId).run();
  return res.meta.changes === 1;
}

export async function denyBooking(db: D1Database, bookingId: number): Promise<boolean> {
  const res = await db.prepare(
    `UPDATE bookings SET status = 'denied', cancelled_at = ? WHERE id = ? AND status = 'requested'`
  ).bind(nowIso(), bookingId).run();
  return res.meta.changes === 1;
}
```

`src/core/availability.ts` を以下の方針で書き換える（関数シグネチャ・戻り値型は不変）:
1. planSlots クエリに `p.duration_min AS durationMin` を追加し、slot_types を `SELECT id, start_time FROM slot_types` で取得して `startMin` マップ（`HH:MM`→分）を作る
2. bookings 集計の WHERE を `b.status IN ('requested', 'confirmed')` に変更（列は現状どおり）。`plansBookedInSlot` は `Map<date, Array<{ planId, slotTypeId }>>` に変更（同一date内の全予約枠を保持）
3. capacity_overrides を範囲で取得: `SELECT date, plan_id AS planId, slot_type_id AS slotTypeId, capacity FROM capacity_overrides WHERE date BETWEEN ?1 AND ?2` → Map
4. 各 (date, ps) の判定:
   - manual_closed: 現状どおり
   - 有効定員 `cap = override ?? ps.capacity`
   - linked: その date の予約枠一覧から、`!(entry.planId === ps.planId && entry.slotTypeId === ps.slotTypeId)` かつ リソース共有 かつ `overlap(start[entry.slot], dur[entry.plan], start[ps.slot], dur[ps.plan])` を満たす entry の planId 集合（重複除去・昇順）。空でなければ linked_closed
   - full: `booked >= cap` / open: それ以外
   - 出力の `capacity` は有効定員、`remaining = max(0, cap - booked)`

overlap は `aStart < bStart + bDur && bStart < aStart + aDur`。

- [ ] **Step 4: 全テスト green 確認** — Run: `npm test && npm run typecheck`
（注意: 既存 `availability.test.ts` の「コース連動」テストで B@10:30 が新たに linked_closed になるが、既存assertには影響しない。既存 booking.test の「別時間帯のプランBは予約可能」= A@09:00 と B@13:00 は重ならないので引き続き成功する）

- [ ] **Step 5: Commit** — `feat: コアv2(時間重なり連動・日別定員上書き・リクエスト予約の承認/否認)`

---

### Task 3: 予約フォームの内訳対応（大人/小人・単価・プラン編集）

**Files:**
- Modify: `src/routes/admin/calendar.tsx`, `src/routes/admin/plans.tsx`, `test/admin-day.test.ts`, `test/admin-edit.test.ts`, `test/admin-plans.test.ts`

- [ ] **Step 1: テストを更新（先に書き換えて失敗を確認）**

`test/admin-day.test.ts` の変更点:
- 新規予約POSTのフォームデータ: `party_size: '3'` → `num_adults: '2', num_children: '1'`。DB検証を `SELECT customer_name, party_size, num_adults, num_children, created_by ...` にし、`expect(row?.party_size).toBe(3); expect(row?.num_adults).toBe(2); expect(row?.num_children).toBe(1);` とする
- 連動クローズPOST・入力不備POSTも同様に `num_adults`/`num_children` フィールドへ（入力不備テストは `num_adults: '0', num_children: '0'` で合計0を不備とする方に変更してもよいが、customer_name 空のままでもよい — customer_name 空のまま維持すること）
- total_amount 自動計算の新テストを1件追加:
```ts
  it('total_amount を空にするとプラン単価から自動計算される（大人2小人1）', async () => {
    const res = await app.request('/admin/bookings', {
      ...form({
        plan_id: String(PLAN_A), slot_type_id: String(SLOT_AM), date: D,
        customer_name: '自動計算', customer_phone: '', num_adults: '2', num_children: '1',
        total_amount: '', payment_method: 'onsite_cash', notes: ''
      }),
      headers: { ...form({}).headers, cookie }
    }, env);
    expect(res.status).toBe(302);
    const row = await env.DB.prepare(`SELECT total_amount, price_adult, price_child FROM bookings WHERE customer_name = '自動計算'`)
      .first<{ total_amount: number; price_adult: number; price_child: number }>();
    expect(row).toEqual({ total_amount: 8000 * 2 + 4000 * 1, price_adult: 8000, price_child: 4000 });
  });
```
- 日別ページ表示テストに `expect(html).toContain('大2');` を追加（予約一覧の人数列は `大{num_adults}小{num_children}` 形式で表示）

`test/admin-edit.test.ts` の変更点:
- フォームデータ `party_size: '2'` → `num_adults: '2', num_children: '0'`
- 「編集フォームに現在の値」テスト: `value="2"` はそのまま（num_adults入力のvalue）

`test/admin-plans.test.ts` の変更点:
- 一覧テスト: `expect(html).toContain('8000');` はそのまま。追加で `expect(html).toContain('120分');`（所要時間表示）
- 新規作成POST: フィールド `price` → `price_adult: '5000', price_child: '2500', duration_min: '90'`。DB検証を `SELECT name, price_adult, price_child, duration_min, active FROM plans ...` に変更し、期待値 `{ name: '新プラン', price_adult: 5000, price_child: 2500, duration_min: 90, active: 1 }`
- 編集フォームテスト: `value="プランA"` はそのまま。追加: `value="8000"`（大人料金）と `value="120"`（所要時間）
- 更新POST: `price: '9000'` → `price_adult: '9000', price_child: '4500', duration_min: '120'`。DB検証 `SELECT name, price_adult FROM plans` 期待 `{ name: 'プランA改', price_adult: 9000 }`
- 「リソース変更」「催行を外す」テストのフォームも同様に `price_adult`/`price_child`/`duration_min` フィールドへ

- [ ] **Step 2: 失敗確認** — Run: `npm test -- "admin-(day|edit|plans)"` → FAIL

- [ ] **Step 3: 実装（コントラクト）**

`calendar.tsx`:
- 新規予約フォーム: `party_size` 入力を `大人人数 num_adults`（number, min=0, value=1）と `小人人数 num_children`（number, min=0, value=0）に置き換え。`total_amount` のラベルに「空欄でプラン単価から自動計算」と注記
- POST `/bookings`: `num_adults`/`num_children` を0以上の整数として検証、合計1以上必須。プランの `price_adult`/`price_child`/存在 を取得し、`total_amount` が空文字なら `numAdults * price_adult + numChildren * price_child` を採用。`priceAdult`/`priceChild` はプラン値をスナップショット
- 予約一覧の人数列: `大{num_adults}小{num_children}`（例: 大2小1）
- 編集フォーム: `num_adults`/`num_children` 入力（現値）。POST `/bookings/:id` も同様に検証し `changeBooking` へ `numAdults`/`numChildren` を渡す（total_amount は入力値そのまま。空文字なら**予約に保存済みの単価スナップショット**から自動計算）

`plans.tsx`:
- 一覧: 価格列を `大人{price_adult}/小人{price_child}` 表示にし、所要時間列 `{duration_min}分` を追加
- 新規作成フォーム: `name`, `price_adult`, `price_child`, `duration_min`（number, min=1, value=120）。INSERT に3列を含める
- 編集フォーム: `price_adult`, `price_child`, `duration_min` 入力（現値）。UPDATE に3列を含める。検証: それぞれ0以上（duration_minは1以上）の整数

- [ ] **Step 4: 全テスト green 確認** — Run: `npm test && npm run typecheck`
- [ ] **Step 5: Commit** — `feat: 予約・プランの大人/小人料金内訳と所要時間の入力対応`

---

### Task 4: 承認フローと日別定員のUI

**Files:**
- Modify: `src/routes/admin/calendar.tsx`, `src/routes/admin/ui.tsx`
- Create: `test/admin-requests.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

`test/admin-requests.test.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { env } from 'cloudflare:test';
import app from '../src/index';
import { adminCookie } from './helpers';
import { seedBasic, makeBooking, PLAN_A, PLAN_B, SLOT_AM } from './fixtures';
import { createBooking } from '../src/core/booking';

const D = '2026-08-01';

function form(data: Record<string, string>, cookie: string) {
  return {
    method: 'POST',
    headers: { 'content-type': 'application/x-www-form-urlencoded', cookie },
    body: new URLSearchParams(data).toString()
  };
}

describe('admin requests & capacity', () => {
  let cookie: string;
  beforeEach(async () => {
    await seedBasic(env.DB);
    cookie = await adminCookie();
  });

  it('承認待ち一覧にリクエスト予約が表示される', async () => {
    await createBooking(env.DB, makeBooking({ status: 'requested', customerName: 'リクエスト客' }));
    const res = await app.request('/admin/requests', { headers: { cookie } }, env);
    expect(res.status).toBe(200);
    const html = await res.text();
    expect(html).toContain('リクエスト客');
    expect(html).toContain('承認');
    expect(html).toContain('否認');
  });

  it('承認するとconfirmedになり、日別ページへ戻れる', async () => {
    const req = await createBooking(env.DB, makeBooking({ status: 'requested' }));
    if (!req.ok) throw new Error('setup failed');
    const res = await app.request(`/admin/bookings/${req.bookingId}/approve`, form({ back: '/admin/requests' }, cookie), env);
    expect(res.status).toBe(302);
    expect(res.headers.get('location')).toBe('/admin/requests?ok=approved');
    const row = await env.DB.prepare(`SELECT status FROM bookings WHERE id = ?`).bind(req.bookingId).first<{ status: string }>();
    expect(row?.status).toBe('confirmed');
  });

  it('否認すると denied になり枠が戻る', async () => {
    const req = await createBooking(env.DB, makeBooking({ status: 'requested' }));
    if (!req.ok) throw new Error('setup failed');
    const res = await app.request(`/admin/bookings/${req.bookingId}/deny`, form({ back: `/admin/day/${D}` }, cookie), env);
    expect(res.status).toBe(302);
    expect(res.headers.get('location')).toBe(`/admin/day/${D}?ok=denied`);
    expect((await createBooking(env.DB, makeBooking({ planId: PLAN_B, customerName: '別件' }))).ok).toBe(true);
  });

  it('日別ページに承認待ちの行が「リクエスト」表示され、承認/否認ボタンがある', async () => {
    await createBooking(env.DB, makeBooking({ status: 'requested', customerName: 'リクエスト客' }));
    const res = await app.request(`/admin/day/${D}`, { headers: { cookie } }, env);
    const html = await res.text();
    expect(html).toContain('リクエスト客');
    expect(html).toContain('リクエスト');
    expect(html).toContain('/approve');
    expect(html).toContain('/deny');
  });

  it('日別定員を上書きでき、空欄で上書き解除できる', async () => {
    // 上書き設定
    let res = await app.request('/admin/capacity', form({
      date: D, plan_id: String(PLAN_A), slot_type_id: String(SLOT_AM), capacity: '2'
    }, cookie), env);
    expect(res.status).toBe(302);
    expect(res.headers.get('location')).toBe(`/admin/day/${D}?ok=capacity`);
    const row = await env.DB.prepare(`SELECT capacity FROM capacity_overrides WHERE date = ? AND plan_id = ? AND slot_type_id = ?`)
      .bind(D, PLAN_A, SLOT_AM).first<{ capacity: number }>();
    expect(row?.capacity).toBe(2);
    // 日別ページに反映（残2）
    const page = await app.request(`/admin/day/${D}`, { headers: { cookie } }, env);
    expect(await page.text()).toContain('残2');
    // 空欄で解除
    res = await app.request('/admin/capacity', form({
      date: D, plan_id: String(PLAN_A), slot_type_id: String(SLOT_AM), capacity: ''
    }, cookie), env);
    expect(res.status).toBe(302);
    const n = await env.DB.prepare(`SELECT COUNT(*) AS n FROM capacity_overrides`).first<{ n: number }>();
    expect(n?.n).toBe(0);
  });
});
```

- [ ] **Step 2: 失敗確認** — Run: `npm test -- admin-requests` → FAIL

- [ ] **Step 3: 実装（コントラクト）**

`calendar.tsx` に追加:
- **GET `/requests`**（Layout title 承認待ち）: `status = 'requested'` の予約を日付昇順で一覧（列: 参加日/時刻/プラン/顧客/人数(大X小Y)/金額/代理店/申込日時）。各行に 承認フォーム（POST `/admin/bookings/{id}/approve`、hidden `back=/admin/requests`、ボタン `承認`）と 否認フォーム（`/deny`、ボタン `否認`）。`?ok=approved`→`承認しました` / `?ok=denied`→`否認しました` を `msg-ok` で表示。0件なら `承認待ちはありません`
- **POST `/bookings/:id/approve`** と **`/bookings/:id/deny`**: hidden `back`（`/admin/` で始まらない場合は `/admin/requests` にフォールバック）。`approveBooking`/`denyBooking` → true: `{back}?ok=approved|denied` / false: `{back}?error=invalid`
- **日別ページ**: 予約一覧で `status='requested'` の行は状態列を `リクエスト` と表示し、承認/否認フォーム（hidden back=`/admin/day/{date}`）を操作列に追加。denied の行は `否認` と表示（キャンセル済みと同様の扱い、操作なし）
- **POST `/capacity`**: `date`（形式検証）、`plan_id`/`slot_type_id` 正整数、`capacity` は空文字 or 0以上の整数。空文字 → `DELETE FROM capacity_overrides WHERE date=? AND plan_id=? AND slot_type_id=?`。数値 → `INSERT INTO capacity_overrides (date, plan_id, slot_type_id, capacity) VALUES (?, ?, ?, ?) ON CONFLICT(date, plan_id, slot_type_id) DO UPDATE SET capacity = excluded.capacity` → `/admin/day/{date}?ok=capacity`（`?ok=capacity` → `定員を更新しました`）
- **日別ページの空き状況表**: 各セルの下に定員上書きフォーム（number input `capacity`、現在の上書き値があればvalue、なければ空。hidden date/plan_id/slot_type_id、ボタン `定員`）。フォームは小さくてよい

`ui.tsx`: ナビに `承認待ち`（/admin/requests）リンクを追加（プラン の前）。

- [ ] **Step 4: 全テスト green 確認** — Run: `npm test && npm run typecheck`
- [ ] **Step 5: Commit** — `feat: リクエスト予約の承認/否認UIと日別定員の上書きUI`

---

### Task 5: 仕上げ（README・E2E・マージ）

- [ ] **Step 1: README** — 「管理画面の機能」に `承認待ち（リクエスト予約の承認/否認）` と `日別定員の上書き` を追記し、プラン管理の行を `プラン管理: 大人/小人料金・所要時間・リソース割当・時間帯別定員` に更新。開発ステップに `2.5 モデル拡張（このリポジトリの現状）` を追記
- [ ] **Step 2: 検証** — `npm test && npm run typecheck` 全green
- [ ] **Step 3: Commit** — `docs: README更新(ステップ2.5完了)`
- [ ] **Step 4: コントローラによるE2E**（ローカルDBリセット→シード→wrangler devで 重なり連動・定員上書き・リクエスト承認 の一連を確認）→ mainへマージ→push

## 完了条件

- 全テストgreen（既存68±調整 ＋ core-v2 13件 ＋ requests/capacity 5件 ＋ フォーム関連の追加）
- スペックv2 §4 の①〜④がテストで検証されている
- E2E: A@09:00予約 → B@10:30が予約不可 → 日別定員2に変更 → 3人目が入らない → リクエスト予約 → 承認待ち一覧 → 承認、の一連が実機で動く
